<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <RootNamespace>MgsvModBldr.Tools.Twpf</RootNamespace>
    <AssemblyName>MgsvModBldr.Tools.Twpf</AssemblyName>
  </PropertyGroup>
  <!-- TwpfXmlTool port: .twpf weather-parameter files <-> XML. Reuses
       Core.CityHash64 for the StringId StrCode (matches the game's
       hashing, same as Atvaark's CityHash dep). -->
  <ItemGroup>
    <ProjectReference Include="..\..\mgsv_modbldr\csgui\MgsvModBldr.Core\MgsvModBldr.Core.csproj" />
  </ItemGroup>
</Project>
