<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <RootNamespace>MgsvModBldr.Tools.Twpf</RootNamespace>
    <AssemblyName>MgsvModBldr.Tools.Twpf</AssemblyName>
  </PropertyGroup>
  <!-- TwpfXmlTool port: .twpf weather-parameter files <-> XML. Reuses
       GameCityHash for the StringId StrCode (matches the game's
       hashing, same as Atvaark's CityHash dep). -->
  <ItemGroup>
    <ProjectReference Include="..\MgsvModBldr.Tools.GameHashing\MgsvModBldr.Tools.GameHashing.csproj" />
  </ItemGroup>
</Project>
